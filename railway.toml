[build]
builder = "dockerfile"
dockerfilePath = "src/SteamViewer.Server/Dockerfile"
watch_paths = ["src/SteamViewer.Server/**"]

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath = "/health"
healthcheckTimeout = 30
