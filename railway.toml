[build]
builder = "nixpacks"
buildCommand = "cd src/SteamViewer.Server && dotnet publish -c Release -o ../../out"

[deploy]
startCommand = "cd out && dotnet SteamViewer.Server.dll"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
